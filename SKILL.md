---
name: data-hub-ui-components
description: Select and verify React UI components in business repositories when the component choice is open. Use to decide whether the installed or latest @zjlab-fe/data-hub-ui satisfies each UI intent and when to fall back to business components, antd, or custom code. Do not use when the user has already fixed every data-hub-ui component choice, or when developing or publishing data-hub-ui itself.
---

# Data Hub UI Components

这个 skill 服务于业务仓的 UI 选型与实现。目标是让每个业务意图优先使用当前版本真实可用的 data-hub-ui 能力；只有确实无法满足时，才按固定顺序降级。

## 核心边界

1. Catalog 只负责发现候选组件。公开 exports、`.d.ts`、README 和 Demo 共同决定当前安装版本能否真实满足需求。
2. 当前安装版本已经满足的意图直接使用，不因为 latest 更新而升级。
3. 普通的实现或重构请求不授权 Agent 安装、升级或降级 data-hub-ui。需要新版本时，默认停止并提示用户安装。
4. 安装失败只表示依赖变更没有完成，不表示 latest 缺少能力。失败后不得自行重试、换参数、换包管理器、换版本、降级实现或继续写代码。
5. 用户明确指定的组件、版本、安装方式或降级方案优先于默认流程，但不能从含糊描述中推断这些授权。

## Step 1：确认范围并拆分业务意图

1. 判断任务是分析、评审、实现还是重构。分析和评审任务只输出结论；实现和重构任务只能在用户授权范围内修改业务代码。
2. 把需求拆成“最小可独立选型能力”，下文简称业务意图。业务意图描述这一部分要解决的问题，不能只写准备使用的组件名称，并按以下标准确定拆分粒度：
   - 每个意图必须能够得到唯一的状态、主要实现来源、候选组件和版本结论；如果其中任一项需要填写多个不同答案，必须继续拆分。
   - 同一页面区域中的专业能力与通用界面外壳需要分别选型。例如上传能力与承载它的步骤条、弹窗外壳可以来自不同组件来源，不能因为视觉上处于同一弹窗就合并为一个意图。
   - 一个组件只解决它实际覆盖的部分。组件只能覆盖原意图的一部分时，必须把已覆盖部分和剩余部分拆开，不能创建“部分解决”状态。
   - 从用户文字、图片和已明确的交互中逐项提取可见控件、数据展示、状态和操作。需求中已经出现删除、覆盖、撤销等危险操作时，操作入口与执行前确认需要分别检查；不能把确认能力隐藏在表格或菜单意图中。
3. 为每个意图维护独立状态，初始状态为 `未解决`。后续只能根据实际检查结果依次转换：
   - 当前安装版本的公开 API 验证通过后，标记为 `当前 data-hub-ui 已解决`。
   - 当前版本没有候选或公开 API 无法满足时，标记为 `待查询 latest`，必须进入 Step 4，不能直接归属业务公共组件、antd 或自定义实现。
   - 实际读取 latest Catalog 后，有候选时标记为 `latest 有候选（待安装或验证）`；没有候选、读取失败或用户明确跳过升级时，标记为 `允许降级`。
   - latest 版本完成安装且公开 API 验证通过后，标记为 `latest data-hub-ui 已解决`；进入 Step 6 的意图则最终标记为 `业务公共组件已解决`、`antd 已解决` 或 `需要自定义实现`。
4. `未解决`、`待查询 latest` 和 `latest 有候选（待安装或验证）` 都是中间状态。除非后续步骤要求等待用户安装或请求方向，否则在所有意图都获得最终归属前，不得结束选型流程。

### 选型结果反馈

1. 分析或评审结束前、Step 5 因依赖门禁暂停前，以及实现完成后的报告中，都必须逐项给出选型结果，不能只概括需要升级的组件或页面级方案。
2. 使用包含“最小业务意图、需求依据、当前状态、实现来源或候选、版本、判断依据或下一步”的表格或等价结构。每一项最小业务意图单独一行；只有实现来源、状态和版本完全一致的普通界面基础能力才可以合并。
3. 需求依据应说明该意图来自用户文字、图片中的可见元素、现有明确行为还是必要推断。推断出的交互必须标明，不得当作用户已经明确要求的事实。
4. 即使流程因等待用户安装、latest Catalog 不可用或需要用户决定而暂停，也要反馈全部已识别意图，包括当前版本已解决、latest 有候选、允许降级和仍在等待处理的项，不能只报告导致暂停的组件。

### 用户已经指定组件时

1. 用户已经指定全部 data-hub-ui 组件且没有要求重新评估时，不运行 Catalog 选型，直接检查当前版本的公开 API 并按指定组件实现。
2. 用户只指定部分组件时，保留这些选择，只对剩余意图运行本流程。
3. 指定组件在当前版本不存在或不能满足需求时，报告具体缺口；未经用户同意，不得改选组件、升级或降级。

## Step 2：确认实际安装状态

1. 读取业务仓 `package.json`、锁文件和已安装包，确认是否安装 `@zjlab-fe/data-hub-ui` 及实际版本 `I`。依赖声明中的 semver 范围不能代替实际解析版本。
2. 识别仓库使用的 npm、pnpm 或 yarn。后续即使获得安装授权，也必须沿用现有包管理器。
3. 已安装时进入 Step 3；未安装时将所有未解决意图标记为 `待查询 latest`，跳到 Step 4。

## Step 3：优先验证当前安装版本

### 3.1 读取当前 Catalog

1. 从已安装包读取 `catalog.json`，并确认其中的 `packageVersion` 等于 `I`。版本不一致的 Catalog 不能用于判断命中。
2. 旧版本没有 Catalog 时，可以通过该安装包的公开 exports 和 `.d.ts` 做兼容发现，但不能因为 Catalog 缺失就断言组件不存在。

### 3.2 匹配并验证候选组件

1. 对每个未解决意图检索 Catalog 的 `name`、`description`、`keywords` 和 `useWhen`；命中 `avoidWhen` 的候选必须排除。当前 Catalog 没有候选时，将该意图标记为 `待查询 latest` 并进入 Step 4。
2. Catalog 条目表示组件模块或能力族，不一定对应单个 React 导出。`relatedExports` 只是提示，当前安装包的声明文件才是事实来源。
3. 通过安装包 `package.json` 的 `types` 或 `typings` 定位根声明，检查根 `index.d.ts` 和必要的组件模块 `index.d.ts`，选择实现该意图所需的具体组件、Provider、Hook 或命令式 API。
4. 验证所选公开导出的 Props、回调和返回值，并从 README、Demo 和类型确认状态归属、生命周期、组合方式及 Provider、React、antd 等运行约束。
5. 必须使用发布包的公开入口；如果实现依赖源码路径、未公开模块或超出组件职责，当前候选不能解决该意图。

例如，Catalog 命中 `FileUploader` 只表示文件上传能力族可作为候选。标准界面可以选择 `FileUploader`；自定义界面则需要从当前版本真实公开的 `UploadProvider`、`useUpload` 和展示子组件中按需选择。

验证通过的意图标记为 `当前 data-hub-ui 已解决`。验证失败时记录具体 API 缺口，将该意图标记为 `待查询 latest`，并只让该意图进入 Step 4。

## Step 4：为待查询 latest 的意图执行查询

1. 必须使用可用的 HTTP 或 Web 工具实际请求 `https://haina-datahub.zero2x.org/data-hub-ui/catalog.json`。收到响应并解析出 `packageVersion` 后，记录明确目标版本 `V`；不能用浮动的 `latest` 标签代替 `V`，也不能根据本地旧 Catalog 或模型知识猜测 latest 的能力。
2. 实际请求失败时，报告“latest Catalog 不可用”及错误。没有发起请求不等于 latest 不可用，也不能表述成最新版没有组件。普通任务可以在披露该限制后将相关意图标记为 `允许降级` 并进入 Step 6；用户要求必须优先确认 data-hub-ui 时保持中间状态，停止并请求方向。
3. 如果 `I` 等于 `V`，当前版本已经是 latest；将仍未解决的意图标记为 `允许降级` 并进入 Step 6。
4. 按 Step 3.2 的 Catalog 匹配规则检查相关意图。没有候选时标记为 `允许降级` 并进入 Step 6；有候选时标记为 `latest 有候选（待安装或验证）`，记录候选组件和版本 `V`，进入 Step 5。
5. 分析或评审任务只报告升级建议。未安装 `V` 前，不能声称 latest 的具体 API 已经通过本地验证。

## Step 5：执行依赖安装门禁

### 未获得安装授权

普通实现或重构请求不包含安装权限。先按“选型结果反馈”完整列出所有业务意图，再报告当前安装状态（未安装或实际版本 `I`）、目标版本 `V`、候选组件和使用新版本的原因，提示用户先自行安装 `@zjlab-fe/data-hub-ui@V`，然后结束当前回合。在确认安装前，不修改依赖文件，也不编写依赖版本 `V` 的业务代码。

### 用户明确要求 Agent 安装

1. 只能使用业务仓现有包管理器和标准方式，对明确版本 `V` 执行一次安装尝试。除非用户同时明确指定，否则不能改变解析参数、其他依赖、包管理器或目标版本。
2. 安装命令成功后，通过锁文件、解析结果或已安装包的 `package.json` 确认实际版本为 `V`，再按 Step 3.2 验证 latest 候选，并复核本次任务中原本由旧版本解决的组件。验证通过的意图标记为 `latest data-hub-ui 已解决`；公开 API 仍不能满足的意图记录具体缺口，标记为 `允许降级` 并进入 Step 6。
3. 安装失败时立即结束当前回合。只报告目标版本、实际命令、关键错误和可能留下的依赖文件变化；不得重试、回滚、进入 Step 6 或继续实现。

用户后续确认已经自行安装，或者明确改变版本与降级方案时，按新的用户指令重新开始：先回到 Step 2 检查实际状态，不能仅凭口头确认假定安装成功。

## Step 6：按顺序处理剩余意图

只有已经标记为 `允许降级` 的意图可以进入本步骤。允许降级的依据只能是：当前版本和实际查询的 latest 都没有候选；版本 `V` 已安装但公开 API 仍不能满足；latest Catalog 的实际请求失败且用户未要求必须确认；或者用户明确要求跳过升级并降级。`待查询 latest` 或 `latest 有候选（待安装或验证）` 的意图不能自动进入本步骤。

1. 先检索业务仓已有公共组件，检查其实现、公开接口和真实调用位置。页面私有代码、相似 JSX 或名称相近不能算公共组件命中。
2. 没有合适的业务公共组件时，再检查 antd 原生组件或合理组合。antd 能直接满足时，不增加没有业务价值的包装层。
3. data-hub-ui、业务公共组件和 antd 都不能合理满足时才自定义实现，并且只补齐仍未解决的能力。

用户明确要求必须使用 data-hub-ui 时，报告能力或版本阻塞并请求方向，不能静默降级。

## Step 7：实现、验证与报告

1. 按每个业务意图的最终归属组合页面。所有 data-hub-ui 能力只从发布包公开入口导入，不复制 Demo 的源码别名、展示外壳或大段 mock 数据。
2. 保持远程请求、业务状态和错误处理的所有权清晰，不让组件选型改变未授权的业务架构。
3. 检查每个意图都有且只有一个主要实现归属，并确认实际安装版本与所用 Catalog 的 `packageVersion` 一致。
4. 运行与改动相称的类型检查、测试或 Demo 验证，并按“选型结果反馈”逐项报告组件命中、版本决定、降级原因和未完成的验证。

只有真实对比测试证明 Catalog、README、Demo 和类型仍不足以指导某个组件的实现时，才为该组件补充 Recipe 或 Few-shot；不要把组件级 API 教程继续堆入总控 skill。
